# 🚀 Quick Start Guide

## สำหรับการทดสอบ Local

### 1. ติดตั้ง Dependencies

```bash
# ติดตั้ง Tesseract OCR
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
# macOS: brew install tesseract
# Linux: sudo apt-get install tesseract-ocr tesseract-ocr-tha

# ติดตั้ง Python packages
pip install -r requirements.txt
```

### 2. รัน Backend

```bash
python app.py
```

Backend จะรันที่ `http://localhost:5000`

### 3. เปิด Frontend

เปิดไฟล์ `index.html` ในเบราว์เซอร์ หรือใช้:

```bash
# Python HTTP Server
python -m http.server 8000

# จากนั้นเปิด http://localhost:8000
```

### 4. ทดสอบ

1. คลิก "เปิดกล้อง & เริ่มสแกน"
2. จ่อบัตรประชาชนให้ชัดเจน
3. รอระบบสแกนอัตโนมัติ (ทุก 5 วินาที)
4. ตรวจสอบข้อมูลที่ดึงมาได้

---

## สำหรับ Deploy บน Railway (แนะนำ)

### ขั้นตอนที่ 1: เตรียม Repository

```bash
# สร้าง Git repository (ถ้ายังไม่มี)
git init
git add .
git commit -m "Initial commit"

# Push ไปยัง GitHub
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

### ขั้นตอนที่ 2: Deploy บน Railway

1. ไปที่ https://railway.app
2. Sign up/Login ด้วย GitHub
3. คลิก "New Project"
4. เลือก "Deploy from GitHub repo"
5. เลือก repository ของคุณ
6. Railway จะ deploy อัตโนมัติ!

### ขั้นตอนที่ 3: รับ URL

- Railway จะให้ URL เช่น: `https://your-app.railway.app`
- ทดสอบ: `https://your-app.railway.app/api/health`
- เข้าใช้งาน: `https://your-app.railway.app`

**เสร็จแล้ว!** 🎉

---

## 🧪 ทดสอบด้วย Test Script

```bash
# ทดสอบ API
python test_api.py path/to/id-card-image.jpg
```

---

## ⚡ Tips สำหรับความแม่นยำสูงสุด

1. **แสงสว่าง**: ใช้แสงที่เพียงพอและสม่ำเสมอ
2. **มุมกล้อง**: ถือบัตรให้ตรงและขนานกับกล้อง
3. **ความคมชัด**: ให้บัตรอยู่ในโฟกัส ไม่เบลอ
4. **พื้นหลัง**: ใช้พื้นหลังที่เรียบและตัดกันกับบัตร
5. **ระยะห่าง**: ถือบัตรให้เต็มเฟรมพอดี ไม่ใกล้หรือไกลเกินไป

---

## 📱 รองรับอุปกรณ์

- ✅ Desktop (Chrome, Firefox, Edge, Safari)
- ✅ Mobile (Chrome, Safari)
- ✅ Tablet
- ⚠️ ต้องใช้ HTTPS หรือ localhost เท่านั้น

---

## 🆘 ปัญหาที่พบบ่อย

### กล้องไม่เปิด
- ตรวจสอบว่าใช้ HTTPS หรือ localhost
- ตรวจสอบสิทธิ์การเข้าถึงกล้องในเบราว์เซอร์

### OCR ไม่แม่นยำ
- ปรับแสงและมุมกล้อง
- ให้บัตรชัดเจนและเต็มเฟรม
- ลองถ่ายภาพหลายครั้ง

### API Error
- ตรวจสอบว่า backend รันอยู่
- ตรวจสอบ API URL ถูกต้อง
- ดู console log ในเบราว์เซอร์

---

## 📚 เอกสารเพิ่มเติม

- [README.md](README.md) - คู่มือหลัก
- [DEPLOYMENT.md](DEPLOYMENT.md) - คู่มือ deploy แบบละเอียด
- [ThaiPersonalCardExtract](https://github.com/ggafiled/ThaiPersonalCardExtract) - Library ที่ใช้

---

**Happy Coding! 🎉**
